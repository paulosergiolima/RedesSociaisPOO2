public class TextPost extends Post {
    // until changes in Postwithcommentsection is made it will come from post.
    public TextPost(Usuario postUser, String postText) {
        super(postUser, postText);
    }
}
